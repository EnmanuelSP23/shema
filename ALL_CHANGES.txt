ALL CHANGES — SHEMA Makeup Collection Website
=============================================
Date: 2026-09-18

PROJECT OVERVIEW
----------------
Custom e-commerce website for SHEMA — a makeup reselling business.
Brands: Sephora Collection, Victoria's Secret, Juicy Couture, Givenchy, Dolce & Gabbana.
Tech: Next.js 16, TypeScript, Tailwind CSS v4, Supabase, Netlify.

COMMIT HISTORY
--------------

1. 15f05b2 — Initial commit from Create Next App
2. 2125f27 — Initial project setup with Next.js
3. 7ea5ec0 — Add warm cream background and subtle floral/dot textures
4. 4aa6507 — Add complete shopping features: cart, checkout, product catalog
   with filters, 56 product images, and responsive redesign
5. 3a232fe — Migrate to Supabase database, add admin panel with password
   gate, and server-side rendering (LATEST)

FULL CHANGE LOG
---------------

[1] INITIAL SETUP (15f05b2, 2125f27)
- Created Next.js 16 project with App Router
- Configured TypeScript and Tailwind CSS v4
- Basic project scaffolding

[2] VISUAL DESIGN (7ea5ec0)
- Added warm cream background color
- Subtle floral/dot textures for brand aesthetic

[3] SHOPPING FEATURES (4aa6507)
- Product catalog with 30 products and 56 images
- Category filtering (Lip Gloss, Lip Stain, Lip Care, etc.)
- Shopping cart with localStorage persistence
- Checkout page
- Contact page
- Responsive design for mobile/tablet/desktop
- Product detail pages with related products

[4] SUPABASE MIGRATION + ADMIN PANEL (3a232fe) — LATEST
This is the biggest change. Products moved from a static JSON file
to a Supabase PostgreSQL database. An admin dashboard was added.

FILES CREATED:
- src/lib/supabase.ts — Supabase client singleton
- src/lib/products.ts — Server functions (getProducts, getProductById, etc.)
- src/app/admin/layout.tsx — Password gate using env var
- src/app/admin/page.tsx — Admin dashboard with product list table
- src/app/admin/ProductForm.tsx — Create/edit form with image upload
- seed.sql — SQL script to seed 30 products into Supabase
- CHANGES.txt — Detailed changelog for this commit

FILES MODIFIED:
- package.json — Added @supabase/supabase-js dependency
- src/app/page.tsx — Changed to async Server Component (fetches from Supabase)
- src/app/products/page.tsx — Client-side Supabase fetch instead of static import
- src/app/products/[id]/page.tsx — Client-side Supabase fetch for product detail
- AGENTS.md — Updated documentation for admin page and Supabase setup

FILES DELETED:
- src/data/products.json — Data now lives in Supabase database

ARCHITECTURE CHANGE:
  BEFORE: products.json → direct ES import → 3 consumer components
  AFTER:  Supabase DB → server/client fetch → same 3 consumer components
          Supabase Storage ← image uploads from admin form
          /admin route → Supabase CRUD operations

ENVIRONMENT VARIABLES REQUIRED (set in Netlify dashboard):
- NEXT_PUBLIC_SUPABASE_URL — Your Supabase project URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY — Your Supabase anonymous key
- NEXT_PUBLIC_ADMIN_PASSWORD — Password for /admin access

NETLIFY DEPLOYMENT STEPS:
1. Go to Netlify Dashboard → your site → Site settings → Environment variables
2. Add all three env vars listed above
3. Push to git (git push) — Netlify auto-deploys
4. After deploy, visit https://your-site.netlify.app/admin
5. Enter the password you set in NEXT_PUBLIC_ADMIN_PASSWORD

ADMIN PAGE FEATURES:
- Password-protected access (stored in sessionStorage)
- Product list table with image thumbnails
- Add new product
- Edit existing product (pre-filled form)
- Delete product with confirmation
- Image upload to Supabase Storage
- Fields: brand, name, size, color, price, category, description

COMMIT HISTORY (continued)
--------------------------
6. 9051eac — Fix image uploads: add Supabase Storage RLS policies
7. 420f13b — Allow Supabase storage images in Next.js Image component
8. 855e792 — Fix image sizing: use object-contain for proper aspect ratio
9. 42dd39f — Allow custom brand and category input in admin form

[5] FIX IMAGE UPLOADS (9051eac, 420f13b)
Fixed two issues preventing uploaded product images from displaying:

ISSUE 1 — Storage RLS Policy Error:
  Symptom: "Failed to upload image" + "new row violates row-level security policy"
  Fix: Added Supabase Storage policies for the `product-images` bucket:
    - INSERT policy: allows public uploads (bucket_id = 'product-images')
    - SELECT policy: allows public reads (bucket_id = 'product-images')
  Note: Bucket must also be set to Public in Supabase Dashboard → Storage

ISSUE 2 — Next.js Image Component Blocking External Images:
  Symptom: Image upload succeeds but image doesn't render on the page
  Fix: Added `images.remotePatterns` to next.config.ts to whitelist *.supabase.co
  This is required because Next.js blocks external image domains by default.

FILES MODIFIED:
- next.config.ts — Added images.remotePatterns for *.supabase.co

SUPABASE STORAGE SETUP REQUIRED (for new projects):
1. Create a Storage bucket named "product-images" (set to Public)
2. Add these SQL policies via Supabase SQL Editor:
   CREATE POLICY "Allow public uploads" ON storage.objects
     FOR INSERT WITH CHECK (bucket_id = 'product-images');
   CREATE POLICY "Allow public reads" ON storage.objects
     FOR SELECT USING (bucket_id = 'product-images');

[6] FIX IMAGE SIZING (855e792)
Changed product images from object-cover (crops to fill) to object-contain
(fits within container without cropping) for better display of uploaded images.

FILES MODIFIED:
- src/components/ProductCard.tsx — object-cover → object-contain with padding
- src/app/products/[id]/page.tsx — same change on detail page

[7] CUSTOM BRAND & CATEGORY INPUT (42dd39f)
Replaced the fixed <select> dropdowns for Brand and Category in the admin
product form with <input> + <datalist>. This lets the user pick from
existing suggestions or type any custom value — useful when sourcing
products from different brands/categories not in the original list.

FILES MODIFIED:
- src/app/admin/ProductForm.tsx — Brand and Category fields changed from
  <select> to <input list="..."> with <datalist> suggestions

[8] HOMEPAGE IMAGE FIX + PRODUCTS PAGE BUG FIX (2026-09-20)

ISSUE 1 — Hero image on homepage looked bad:
  Symptom: The SHEMA logo image on the right side of the hero section
  had object-contain with no styling, appearing as a flat rectangle.
  Fix: Changed to object-cover with rounded-3xl, shadow-2xl, and adjusted
  height (h-48 on mobile, md:h-[420px] on desktop).

ISSUE 2 — Products page showed "No products found" despite having products:
  Symptom: /products page displayed "Showing 0 of 11 products" and
  "No products found matching your criteria" even with no filters applied.
  Root cause: useMemo dependency array was missing `products`. When the
  page loaded, products was [], filteredProducts was calculated as [].
  When Supabase responded and updated state, useMemo didn't recalculate
  because `products` wasn't in the dependency array.
  Fix: Added `products` to useMemo dependencies.

FILES MODIFIED:
- src/app/page.tsx — Hero image: object-contain → object-cover, added
  rounded-3xl, shadow-2xl, adjusted height for mobile/desktop
- src/app/products/page.tsx — Added `products` to useMemo dependency array

[9] CONTACT PAGE + SOCIAL LINKS (2026-09-20)

Replaced the contact form with a "How to Order" guide and centralized
social media links into a shared config.

WHAT CHANGED:
- Contact form (Send Us A Message) removed — not needed for new business
- Replaced with "How to Order" step-by-step guide (4 steps)
- Added dynamic "Message Us" CTA with Instagram/Facebook toggle
- Social media links now stored in a single config file
- Footer and Contact page both import from config (single source of truth)
- Updated Instagram and Facebook URLs to real accounts

FILES CREATED:
- src/lib/config.ts — Centralized social media URLs

FILES MODIFIED:
- src/components/Footer.tsx — Imports social links from config.ts
- src/app/contact/page.tsx — Replaced form with How to Order + social CTA

[10] REMOVE CART, ADD "GET PRODUCT NOW" (2026-09-20)

No payment system yet, so replaced the Add to Cart flow with a direct
"Get Product Now" button that opens Instagram DM for order processing.

WHAT CHANGED:
- "Add to Cart" button → "Get Product Now" link (opens Instagram DM)
- Cart icon + badge removed from Header
- Dead useCart import removed from ProductCard
- Cart/checkout pages remain in codebase (easy to re-enable later)

FILES MODIFIED:
- src/app/products/[id]/page.tsx — Replaced addItem button with Instagram link
- src/components/Header.tsx — Removed cart icon, badge, and useCart import
- src/components/ProductCard.tsx — Removed dead useCart import
